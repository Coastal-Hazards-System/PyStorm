# Data — large files, not version-controlled
# (anchored with **/ so the rule applies to module-nested data/ trees too)
**/data/inputs/
**/data/outputs/

# Archive — local-only stash of legacy data + superseded docs (not on GitHub)
archive/

# Local-only reports (not on GitHub)
docs/CyHAN-Comment-Standard-v0.3-Audit.md

# Python
__pycache__/
*.pyc
*.pyo
.eggs/
*.egg-info/
dist/
build/

# C++ build artifacts
*.pyd
*.so
*.dylib
_build/

# Editors / IDE / OS
.DS_Store
Thumbs.db
.venv/
*.env
.idea/
.claude/
